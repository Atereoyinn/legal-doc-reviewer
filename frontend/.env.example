# Frontend Environment Variables
# Copy this to .env.local for local development (never commit .env.local)

# Backend API URL (defaults to localhost:8000 in dev, Render URL in production)
VITE_API_URL=http://localhost:8000

# Must match API_KEY set on the backend
VITE_API_KEY=your_strong_random_secret_here
